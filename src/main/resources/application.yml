spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/crud_rest_api_db
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  springdoc:
    swagger-ui:
      path: /swagger-ui/index.html
